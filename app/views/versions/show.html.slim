h1 = @version.name

.mb-2 = link_to(t('helpers.submit.run.new'), new_run_path(version_id: @version.id), class: 'btn btn-primary')

br
= paginate @runs

- @runs.each do |run|
  = render 'runs/card', run: run
